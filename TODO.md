# Refactor Portfolio to Use Single Global Background

## Tasks to Complete
- [ ] Edit src/components/Skills.jsx: Replace `bg-black` with `bg-transparent` in section className
- [ ] Edit src/components/Projects.jsx: Replace `bg-black` with `bg-transparent` in section className
- [x] Edit src/components/Experience.jsx: Replace `bg-black` with `bg-transparent` in section className
- [ ] Edit src/components/Contact.jsx: Replace `bg-black` with `bg-transparent` in section className
- [ ] Edit src/components/Achievements.jsx: Replace `bg-black` with `bg-transparent` in section className
- [ ] Edit src/components/Footer.jsx: Replace `bg-black` with `bg-transparent` in footer className
- [ ] Edit src/App.jsx: Replace `bg-black` with `bg-transparent` in root div className
- [x] Run the app to verify changes
